# Only required for `bdc-lookthrough refresh` (live EDGAR).
# The default MCP server and overlap CLI use the bundled snapshot — no key, no identity.
# https://www.sec.gov/os/webmaster-faq#code-support
EDGAR_IDENTITY=Your Name you@example.com
